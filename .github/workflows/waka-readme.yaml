name: Waka Readme

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  update-readme:
    name: WakaReadme DevMetrics
    runs-on: ubuntu-latest
    steps:
      - name: Get date
        id: date
        run: echo "NOW=$(date +'%Y-%m-%dT%H:%M:%S')" >> $GITHUB_ENV
      - uses: athul/waka-readme@master # this action name
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          SHOW_TITLE: true 
          SECTION_NAME: waka
          BLOCKS: ->
          TIME_RANGE: last_year
          SHOW_TIME: false 
          SHOW_TOTAL: false 
          LANG_COUNT: 5
          CODE_LANG: python
          COMMIT_MESSAGE: "${{env.NOW}} Update WakaTime stats"

